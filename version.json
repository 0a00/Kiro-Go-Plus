{
  "version": "1.2.8",
  "changelog": "Recover complete tool calls when an upstream AWS EventStream ends after valid JSON arguments but omits the final stop marker. Incomplete, empty, or malformed tool arguments remain rejected and retryable.\n当上游 AWS EventStream 已传完合法工具 JSON、但遗漏最终 stop 标记时，安全恢复该工具调用；不完整、空白或格式错误的参数仍会拒绝并进入重试。",
  "download": "https://github.com/0a00/Kiro-Go-Plus"
}
